/**
 * KOROBOS — Session detail page (dynamic route).
 * sessionId is read via useLocalSearchParams inside the screen component.
 */

export { default } from "../../src/screens/LearningSessionDetailScreen";
